#pragma once

#include "khg_phy/phy_types.h"

bool phy_check_point_greater(const phy_vect a, const phy_vect b, const phy_vect c);
bool phy_check_axis(phy_vect v0, phy_vect v1, phy_vect p, phy_vect n);

